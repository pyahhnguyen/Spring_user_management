<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.jwat_g.mapper.AccountMapper">

    <resultMap id="AccountResultMap" type="com.example.jwat_g.model.Account">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password_hash"/>
        <result property="fullName" column="full_name"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByEmail" resultMap="AccountResultMap">
        SELECT * FROM accounts WHERE email = #{email}
    </select>

    <select id="getAccountById" resultMap="AccountResultMap">
        SELECT * FROM accounts WHERE id = #{id}
    </select>

    <insert id="insertAccount" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO accounts (email, password_hash, full_name, role, created_at)
        VALUES (
            #{email},
            #{password},
            #{fullName},
            #{role},
            CURRENT_TIMESTAMP
        )
    </insert>

    <select id="isEmailExist" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM accounts WHERE email = #{email})
    </select>

</mapper>
